import { NavItemFilterPipe } from './nav-item-filter.pipe';

describe('NavItemFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NavItemFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
